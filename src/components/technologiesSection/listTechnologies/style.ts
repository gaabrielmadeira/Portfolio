import styled from "styled-components"

export const StyledListTech = styled.ul`
    width: 100%;
    height: auto;

    display: flex;
    flex-wrap: wrap;
    gap: 10px;
` 

export const StyledLogos = styled.img`
    width: clamp(25px, 10vw, 150px);
    height: clamp(25px, 10vh, 150px);
`